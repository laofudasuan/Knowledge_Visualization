我需要用react搭建一个用户个性化的知识图谱网站：

每个人的知识是一个网状结构，这个网状结构由很多张图组成，每张图上由很多知识和知识之间的关联。用户可以注册和登陆账户，并搭建自己的知识库。同时，用户可以将自己遇到的问题用知识图谱上的动画来呈现。

每个用户的文件结构：

每个用户有三个文件夹；
一：图谱结构文件夹。每张图是一个json文件，包含以下属性：
  - Tag：图谱有哪些标签
  - aliases：图谱的不同名称，用于搜索关键词时更方便找到
  - cssclasses：这张图显示时的样式（是链、图还是树、用什么设计风格）
  - nodes：图中每个点，每个点包含一个必填属性id、和几个选填属性：color表示颜色，如果没有则为默认颜色；x,y表示坐标，如果们没有则由力导向自动生成；link表示点击后跳转的页面，跳转还分为两种，一种是业内跳转，一种是弹出新窗口跳转，旧窗口不变
  - edges：点和点之间的关系，每条边包含source、target、和选填属性label表示边上需要显示的文字
二：文章文件夹。每篇文章以一个markdown文件形式存储。
三：问题文件夹。每个问题包含一个markdown文件+一个动画脚本文件。markdown用于表示问题的内容，动画脚本用于表示问题的解决路径，在前端会有一个播放按钮，点击播放后会自动根据脚本的指示在各个图谱/知识点之间播放切换的动画来展示解决问题的过程

前端页面：

网站主页：
1. 网站主页，讲网站的介绍，作用。右上角有登陆按钮，点击后可以登陆/注册。登陆后网站主页上出现一个”进入我的世界“按钮

个人页面：
分为三个页面，右上角有一个导航栏，用于切换不同的页面。
1. 知识图谱页面
- 如果用户的知识图谱数据库中有root.json，则显示root.json中的内容，否则显示一个默认的图谱，用React-Flow框架展示。
- 页面左上方有一个放大镜图标，点击后弹出搜索框，可以按照所有图谱的文件名和alias搜索图谱，并显示搜索结果，点击搜索结果后可以跳转到该图谱的页面。同时搜索结果的最后一行显示一个添加按钮，表示新建一个以当前搜索内容为文件名的json文件，点击后跳转到添加图谱的页面。
- 每个图谱的右下角有一个编辑按钮，点击后可以进入json文件的编辑模式，可以修改图谱的属性，并保存。
2. 文章页面
- 呈现用户写过的所有文章，按照最近依次保存时间排序。
- 每个文章的标题是文章的文件名，点击后可以跳转到该文章的详情页面。
- 文章详情页面，展示文章的内容，同时右下角有一个编辑按钮，点击后开始markdown文件的编辑。
3. 问题页面
- 呈现用户写过的所有问题，按照最近依次保存时间排序。
- 每个问题的标题是问题的文件名，点击后可以跳转到该问题的详情页面。
- 问题详情页面分为内容/解决两个部分，
  - 问题部分，展示markdown内容，同时右下角有一个编辑按钮，点击后开始markdown文件的编辑。
  - 解决部分为一个动画脚本，可以编辑和播放 

我需要用一个前后端架构来实现这个项目，但我只负责前端，后端交给我的同事。因此帮我按照前后端分开的架构来设计，先实现前端，并把后端的接口告诉我的同事